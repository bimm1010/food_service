import 'package:flutter/material.dart';

class OrderMainScreen extends StatelessWidget {
  const OrderMainScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Order main screen');
  }
}
