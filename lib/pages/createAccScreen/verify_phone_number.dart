import 'package:flutter/material.dart';

class VerifyNumberPhone extends StatelessWidget {
  const VerifyNumberPhone({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('verify number phone'),
    );
  }
}
